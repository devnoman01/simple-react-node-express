/**
 * ----- basic -----
 * 1. create a folder
 * 2. open command line to that folder
 * 3. >> npm init
 *
 * ----- for express/node server -----
 * 1. >> npm install express
 * 2. create index.js file
 * 3. create app variable
 * 4. declare port
 * 5. set app.get('/')
 * 6. listen to port
 * 7. >> node index.js
 *
 * ----- get -----
 * cors middleware
 * require cors
 *
 */
